const assert = require('assert');
const Players = require('../models/players.js');
const FootballTeam = require('../models/footballTeam.js');
// this
describe('FootballTeam', function () {
    let players;
    let footballTeam;
    let hibernian;
// this
    beforeEach(function () {
        players = new Players (name, dateOfBirth, position);
        footballTeams = new FootballTeam
    })

})